# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=agentpay

# API Server
PORT=8080

# Frontend API URL (optional, defaults to /api)
VITE_API_URL=/api
